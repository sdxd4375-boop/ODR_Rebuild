# P0 四个阻断项 · 详细处理方案

> 适用仓库：`G:\Code\open_deep_research`（Windows / PowerShell，Shell 亦可）
> 本文件**仅是处理方案**，不含评估结论。每条方案均为"可交给执行者照做"的粒度：确切文件、确切内容、确切验收命令。

## 0. 执行顺序与总量

| 序 | 阻断项 | 依赖 | 估算 | 备注 |
|---|---|---|---|---|
| ① | **B1** 魔改层入版本控制 | 无 | 0.5d | 与 ② 可并行；必须先做，否则后续改动无基线可回滚 |
| ② | **B2** 环境修复与环境自检 | 无 | 0.3d | 与 ① 并行 |
| ③ | **B3** 冷启动链路（四连环） | ①② | 0.7d | 验收必须跑在 ② 修好的 venv 上 |
| ④ | **B4** 模型与搜索配置（第 5 环） | ③ | 0.3d | 验收依赖 ③ 启动的服务 |

**合计 ≈ 1.8 人日。**

提交策略：B1 只提交**现状**（baseline）；B2/B3/B4 各自一个 commit（B4 的 `utils.py` 补丁单独一个 commit）。

---

## B1 · 魔改层入版本控制

### 完成定义（DoD）

从自建远端 `git clone` 到干净目录，能拿到全部魔改层；`git status --porcelain` 为空；仓库内不含 `.venv`、密钥、用户数据、构建产物。

### 事实基线

- `git add -n -A` → **164 条待加入，其中 82 条在 `.venv/` 内**。
- `.venv/.gitignore` **不存在**，仓库 `.gitignore` **没有 `.venv/` 规则**（`git check-ignore .venv` 无输出）。
- 已正确忽略：`.env`（`.gitignore:32 *.env`）、`web/dist`（`web/.gitignore:11`）、`web/node_modules`（`web/.gitignore:10`）。
- 会误入：`docs.zip`(1)、`.codebuddy/`(1)、`build_pdf/*.pdf`、`data/`（当前不存在，但 docstore 会写入含**用户文档正文+向量**的 `data/docs/docs_index.json`，以及 `data/charts`、`data/exports`）。
- 仓库根残留 `pytest-cache-files-dev9dqem/`、`pytest-cache-files-vnf0oqj7/`（pytest 在系统 temp 不可用时把 basetemp 建在 rootdir），当前用户**无权限删除**（`UnauthorizedAccessException`）。

> ⚠ **修正**：此前的审查记录写的是"确认忽略规则安全（已核实 .env / web/dist 已忽略）"。该表述**不完整** —— `.venv/` 当时未核实且实际**未忽略**。**Step 1 必须在任何 `git add -A` 之前完成**，否则会有 82 个 venv 文件进索引。

### 步骤

**Step 1 先扩 `.gitignore`**

在 `.gitignore` 末尾追加：

```gitignore
# Virtualenv (uv) — 82 files would otherwise be staged
.venv/
.uv-cache/

# Local runtime data: docstore index (user document text + embeddings),
# generated charts and exports. Never commit (user data).
data/

# Build artifacts / local doc snapshots
docs.zip
build_pdf/*.pdf
build_pdf/*.json

# Agent scratch dirs
.codebuddy/

# pytest tmp dirs created when the system temp dir is unusable
pytest-cache-files-*/
```

**Step 2 dry-run 复核（不改动任何东西）**

```powershell
git add -n -A | Measure-Object -Line                                        # 期望：由 164 → 约 75
git add -n -A | Select-String "\.venv"                                      # 期望：0 行
git add -n -A | Select-String "docs\.zip|codebuddy|build_pdf.*\.pdf"        # 期望：0 行
git add -n -A | Select-String "\.env$"                                       # 期望：0 行
```

**Step 3 清掉不可删除的 pytest 残目录（ACL 修复）**

```powershell
foreach ($d in Get-ChildItem -Force -Directory -Filter "pytest-cache-files-*") {
  takeown /F $d.FullName /R /D Y | Out-Null
  icacls $d.FullName /grant "$($env:USERNAME):(F)" /T | Out-Null
  Remove-Item -Recurse -Force $d.FullName
}
```

防复发：给 `pyproject.toml` 加 pytest 配置（同 B2 Step 5），并把 `.pytest_cache/` 已有忽略规则保留。

**Step 4 统一换行策略（消除现有 CRLF 警告）**

新建 `.gitattributes`：

```gitattributes
* text=auto eol=lf
*.png binary
*.pdf binary
*.ipynb -text
```

**Step 5 分三个语义化 commit**

Commit A —— 内核改动 + 依赖（最小、可 review）：

```powershell
git add src/open_deep_research/configuration.py `
        src/open_deep_research/utils.py `
        src/open_deep_research/deep_researcher.py `
        src/open_deep_research/retrievers.py `
        src/open_deep_research/docstore.py `
        pyproject.toml uv.lock .env.example .gitignore .gitattributes
git commit -m "feat(core): pluggable extra retrievers + local docstore; stop swallowing supervisor errors"
```

Commit B —— 自建服务层与迁移：

```powershell
git add src/server tests/server migrations alembic.ini docker-compose.dev.yml uv.toml
git commit -m "feat(server): FastAPI sessions/SSE + Postgres checkpointer, docstore API, charts/export, alembic"
```

Commit C —— 前端、脚本与文档：

```powershell
git add web README.md CLAUDE.md build_pdf/*.py searxng nginx docs
git commit -m "feat(web): session UI; docs: architecture/testing/upgrade-plan set; doc: rewrite CLAUDE.md"
```

注意：`web/dist`、`web/node_modules` 被 `web/.gitignore` 忽略 → 前端产物**不入库**，必须靠构建（README 需写明）。`src/legacy/multi_agent.ipynb` 有已跟踪修改（24 行，疑似执行输出）→ 确认后一并加入 Commit A，或 `git checkout -- src/legacy/multi_agent.ipynb` 丢弃。

**Step 6 建自建远端并推**

```powershell
git remote rename origin upstream          # 保留上游，继续吃 dependabot
git remote add origin <自建仓库地址>
git push -u origin main
```

**Step 7 提交前密钥体检**

```powershell
git grep -n "sk-ws-" ; git grep -n "tvly-" ; git grep -n "service_role"
# 期望：三条命令均无输出
```

### 验收

```powershell
git clone <自建仓库地址> "$env:TEMP\odr-verify"
Set-Location "$env:TEMP\odr-verify"
(Get-ChildItem -Recurse -File src\server).Count        # 期望 > 0（服务层在位）
Test-Path web\src\App.tsx                              # True
Test-Path migrations\versions\0002_usage_ledger.py     # True
Test-Path .env                                         # False（只应有 .env.example）
git status --porcelain                                 # 空
```

### 回滚

- 暂存后反悔：`git reset`（只解暂存，不动工作区）；**严禁在此时用 `git checkout .`**，会丢未提交魔改。
- 误暂存 venv：`git rm -r --cached .venv` 后重做 Step 1。
- commit 前建议整目录备份或 `git worktree add` 留副本。

---

## B2 · 环境修复与环境自检

### 完成定义（DoD）

`python -c "import server.app"` 无输出；`pytest tests/server -q` → **33 passed**；解释器版本在 `.python-version` / `pyproject.toml` / `langgraph.json` 三处一致；`python scripts/env_check.py` 退出码 0。

### 事实基线

- `.venv\Lib\site-packages\aiohttp\` 仅 **6 个文件**，`hdrs.py`、`client.py` 等缺失；`aiohttp-3.14.3.dist-info` 元数据却在 → **安装被中断**。
- `src/open_deep_research/utils.py:10 import aiohttp` 是核心硬依赖 → server 起不来、`langgraph dev` 起不来、`pytest tests/server` 两个文件 collect 即失败。
- 当前解释器 **Python 3.13.15**；`langgraph.json` 写 `python_version: "3.11"`；`pyproject.toml` 写 `requires-python = ">=3.10"`；**无 `.python-version`**。

### 步骤

**Step 1 根因确认（只读）**

```powershell
$sp = ".venv\Lib\site-packages\aiohttp"
(Get-ChildItem $sp -File -Recurse).Count      # 期望 ~60；当前 6 → 确认损坏
Test-Path "$sp\hdrs.py"                       # 期望 True；当前 False
```

**Step 2 修复（按环境选一档）**

A 档 —— 可联网（推荐）：

```powershell
uv cache clean aiohttp
uv sync --reinstall-package aiohttp
```

B 档 —— `uv run` 被 Smart App Control / WDAC 拦截（os error 4551），或 `%LOCALAPPDATA%\uv\cache` 不可写：

```powershell
$env:UV_CACHE_DIR = "G:\Code\open_deep_research\.uv-cache"   # 工作区内缓存，绕开受限路径
uv cache clean aiohttp
uv sync --reinstall-package aiohttp

# 之后统一用解释器直调，避免 uv 的临时 launcher：
python -m uvicorn server.app:app --app-dir src --port 8000
python -m pytest tests/server -q
```

C 档 —— 网络受限（沿用 `uv.toml` 的清华镜像）：

```powershell
$env:UV_DEFAULT_INDEX = "https://pypi.tuna.tsinghua.edu.cn/simple"
uv cache clean aiohttp
uv sync --reinstall-package aiohttp
```

D 档 —— 彻底重建（最可靠，耗时最长）：

```powershell
Remove-Item -Recurse -Force .venv
uv sync
```

**Step 3 解释器版本统一（二选一，必须选一个）**

(a) **推荐** —— 与 `langgraph.json` / LangGraph Platform 部署面对齐：

```powershell
"3.11" | Set-Content .python-version
uv sync
```

(b) 保留 3.13 —— 同时改 `langgraph.json` 为 `"python_version": "3.13"`，并 `"3.13" | Set-Content .python-version`。

**不要保持现状**（3.11 / 3.13 / >=3.10 三处不一致，不同机器会装出不同环境）。选 (a) 会重装全部依赖（数分钟），此后 wheel 走 cp311 版本。

**Step 4 新增环境自检脚本 `scripts/env_check.py`**

```python
"""Fail fast on a broken or misconfigured environment.

Run: python scripts/env_check.py    (exit 0 = ready)
"""
from __future__ import annotations

import importlib
import os
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
FAILURES: list[str] = []
WARNINGS: list[str] = []


def check(label: str, ok: bool, detail: str = "") -> None:
    print(f"[{'OK ' if ok else 'FAIL'}] {label}{' — ' + detail if detail else ''}")
    if not ok:
        FAILURES.append(label)


def warn(label: str, detail: str = "") -> None:
    print(f"[WARN] {label}{' — ' + detail if detail else ''}")
    WARNINGS.append(label)


# 1. interpreter
check("python >= 3.10", sys.version_info >= (3, 10), sys.version.split()[0])
pin_file = ROOT / ".python-version"
if pin_file.exists():
    pin = pin_file.read_text(encoding="utf-8").strip()
    check(f".python-version matches ({pin})", sys.version.split()[0].startswith(pin),
          f"running {sys.version.split()[0]}")
else:
    warn(".python-version missing", 'add "3.11" to pin the interpreter')

# 2. hard dependencies of the core package
for mod in ("aiohttp", "langgraph", "langchain_core", "fastapi", "sqlalchemy", "numpy"):
    try:
        importlib.import_module(mod)
        check(f"import {mod}", True)
    except Exception as exc:  # noqa: BLE001
        check(f"import {mod}", False, repr(exc))

# 3. aiohttp file integrity (catches an interrupted install)
try:
    import aiohttp  # noqa: F401

    pkg = Path(aiohttp.__file__).parent
    missing = [n for n in ("hdrs.py", "client.py", "web.py") if not (pkg / n).exists()]
    check("aiohttp package complete", not missing, f"missing {missing}" if missing else "")
except Exception:  # noqa: BLE001
    pass

# 4. project packages
for mod in ("open_deep_research", "server.app"):
    try:
        importlib.import_module(mod)
        check(f"import {mod}", True)
    except Exception as exc:  # noqa: BLE001
        check(f"import {mod}", False, repr(exc))

# 5. .env + required settings
env_file = ROOT / ".env"
if not env_file.exists():
    warn(".env missing", "cp .env.example .env")
else:
    check(".env present", True)
    try:
        from dotenv import dotenv_values

        vals = dotenv_values(env_file)
    except Exception:  # noqa: BLE001
        vals = {}

    def val(key: str) -> str:
        return (os.environ.get(key) or vals.get(key) or "").strip()

    dsn = val("DATABASE_URL")
    check("DATABASE_URL set", bool(dsn), dsn or "empty")
    if dsn:
        check("DATABASE_URL matches compose (port 5433)", ":5433/" in dsn, dsn)
        check("DATABASE_URL db name is nodr", dsn.rstrip("/").endswith("nodr"), dsn)

    search_api = (val("SEARCH_API") or "tavily").lower()
    if search_api == "tavily":
        check("TAVILY_API_KEY set (SEARCH_API=tavily)", bool(val("TAVILY_API_KEY")),
              "or use SEARCH_API=none + EXTRA_RETRIEVERS=duckduckgo")
    if not val("EXTRA_RETRIEVERS"):
        warn("EXTRA_RETRIEVERS empty", "extra retrievers stay disabled")

    roles = ("RESEARCH_MODEL", "COMPRESSION_MODEL", "FINAL_REPORT_MODEL", "SUMMARIZATION_MODEL")
    if val("OPENAI_BASE_URL"):
        warn("OPENAI_BASE_URL is set",
             "the *_MODEL values must exist on that endpoint: " + ", ".join(roles))
    if val("OPENAI_BASE_URL") and not val("OPENAI_API_KEY").startswith("sk-"):
        warn("OPENAI_API_KEY looks empty/unset",
             "openai:* models read OPENAI_API_KEY — a custom base URL still needs it")

# 6. frontend artifact
dist = ROOT / "web" / "dist" / "index.html"
check("web/dist built", dist.exists(), "" if dist.exists() else "run: cd web && npm run build")

print()
if FAILURES:
    print(f"FAILED: {len(FAILURES)} check(s) — " + "; ".join(FAILURES))
    sys.exit(1)
print(f"Environment ready ({len(WARNINGS)} warning(s)).")
```

**Step 5 固定 pytest 行为（顺带消除 B1 的残目录问题）**

`pyproject.toml` 追加：

```toml
[tool.pytest.ini_options]
testpaths = ["tests/server"]
addopts = "-q"
```

**Step 6 README 增加「环境自检」小节**

```markdown
### 环境自检（30 秒，先跑这个）

    uv sync
    python scripts/env_check.py

输出中出现任何 `[FAIL]` 就先按下面对应的提示修好，再继续后面的步骤。
```

### 验收

```powershell
python scripts/env_check.py         # 期望 exit 0，无 [FAIL]
python -c "import server.app"       # 期望无输出
python -m pytest tests/server -q    # 期望 33 passed
```

### 回滚

venv 是纯产物：`Remove-Item -Recurse -Force .venv; uv sync`。`.python-version`、`scripts/env_check.py`、`pyproject.toml` 的 pytest 段可单文件 `git checkout` 撤销。

---

## B3 · 冷启动链路（四连环）

### 完成定义（DoD）

从零（含 `down -v`）执行固定命令序列后：`/api/health` 返回 `db=true, graph=true`；`alembic upgrade head` 幂等；一次受控真研究产出 `done` 事件而非 `error`。

### 事实基线

1. `src/server/` **零处 `load_dotenv`**（全目录 grep 确认）；`load_dotenv` 只在 `migrations/env.py:17`、`tests/run_evaluate.py:9`、`tests/extract_langsmith_data.py:10`。
2. DSN 三处不一致：`.env.example:21`(5432/odr) / `.env:13`(5432/nodr) / `docker-compose.dev.yml:8-12`(5433/nodr) —— 用户、密码、端口、库名全错。
3. `alembic.ini:89` `sqlalchemy.url = driver://user:pass@localhost/dbname` 为真值 → `migrations/env.py:25` 的注入分支恒 False → 迁移失效。
4. `alembic.ini:21` `prepend_sys_path = .` → `migrations/env.py:14 from server import models` 依赖 venv 内 editable 安装；未 `uv sync` 即 ImportError。
5. **【新发现陷阱】** `src/server/db.py:61` 在 `AUTH_MODE=local` 下 `create_all` 建表，**不写 `alembic_version`**；而 `odr_pgdata` 卷持久化 → 曾启动过 server 的库再跑 `alembic upgrade head`，会在 0001 的 `create_table` 报 `DuplicateTable`。已核对 `0001_initial.py` / `0002_usage_ledger.py` 与 `models.py` 字段**完全一致** → 对存量库 `alembic stamp head` 安全且正确。

### 步骤

**Step 1 定义单一事实来源：以 compose 为准**

`docker-compose.dev.yml:8-12` 不动。统一 DSN = `postgresql+asyncpg://postgres:nodr@localhost:5433/nodr`。

`.env.example` 第 15–21 行整段替换为：

```dotenv
# ---------------------------------------------------------------------------
# Self-hosted server (src/server) — frontend + session persistence
# ---------------------------------------------------------------------------
# Canonical DSN. MUST match docker-compose.dev.yml:
#   user postgres / password nodr / db nodr / host port 5433
# The LangGraph checkpointer derives its own psycopg DSN from this value by
# stripping "+asyncpg" (src/server/db.py:34-37).
#   docker compose -f docker-compose.dev.yml up -d postgres
DATABASE_URL=postgresql+asyncpg://postgres:nodr@localhost:5433/nodr
```

本地 `.env` 第 13 行手工改成同一行（`.env` 不入库，需人工同步）。

同时修正 `docker-compose.dev.yml:1-2` 的过期注释（"PostgreSQL only" 但文件里还有 redis/searxng/nginx）。

**Step 2 server 启动加载 `.env`（幂等 + 路径无关）**

`src/server/deps.py` 文件头 import 区补 `from pathlib import Path`，文件末尾追加：

```python
_env_loaded = False


def ensure_env_loaded() -> None:
    """Load the repository .env into os.environ.

    Idempotent, and never overrides variables already present in the real
    environment (override=False), so shell exports and container env win.

    Resolution order:
      1. $ODR_ENV_FILE when set (explicit override for tests / deployments)
      2. <repo root>/.env, located relative to THIS file — not the cwd, so the
         server behaves the same however it is launched
      3. python-dotenv's upward search as a fallback
    """
    global _env_loaded
    if _env_loaded:
        return

    from dotenv import load_dotenv

    repo_root = Path(__file__).resolve().parents[2]  # src/server/deps.py -> repo root
    explicit = os.environ.get("ODR_ENV_FILE")
    if explicit:
        load_dotenv(explicit, override=False)
    elif (repo_root / ".env").exists():
        load_dotenv(repo_root / ".env", override=False)
    else:
        load_dotenv(override=False)
    _env_loaded = True
```

`src/server/app.py`：

```python
from server import db, deps, graphs          # 原: from server import db, graphs + from server.deps import auth_mode
...
@asynccontextmanager
async def lifespan(app: FastAPI):
    """Initialize DB + graph on startup; record readiness flags for /api/health."""
    deps.ensure_env_loaded()                 # ← 必须在 init_engine() 之前
    app.state.db_ready = await db.init_engine()
    app.state.graph_ready = await graphs.manager.start()
    logger.info(
        "Startup complete (db=%s, graph=%s, auth_mode=%s)",
        app.state.db_ready, app.state.graph_ready, deps.auth_mode(),
    )
```

**Step 3 激活 alembic**

`alembic.ini`：

```ini
# line 21 —— 指向 src，使 migrations/env.py 的 `from server import models`
# 不再依赖 venv 里的 editable 安装
prepend_sys_path = %(here)s/src

# line 89 —— 留空，让 migrations/env.py:25-32 的 DATABASE_URL 注入分支生效
sqlalchemy.url =
```

**Step 4 处理 create_all 与 alembic 的双写路径**

`src/server/db.py:66` 的日志改为自解释：

```python
        logger.info(
            "Business tables ensured via create_all (dev mode, no alembic_version "
            "stamp). If you later adopt migrations on this database, run: "
            "alembic stamp head  (then alembic upgrade head for new revisions)"
        )
```

README 中写明两条路径（见 Step 6）。

**Step 5 一键冷启动脚本 `scripts/dev_up.ps1`**

```powershell
# Cold start: Postgres -> migrate -> server
docker compose -f docker-compose.dev.yml up -d postgres
do {
    Start-Sleep -Seconds 2
    $health = docker inspect --format '{{.State.Health.Status}}' odr_postgres
} until ($health -eq 'healthy')

uv run alembic upgrade head          # 存量库改：uv run alembic stamp head
python -m uvicorn server.app:app --app-dir src --port 8000
```

**Step 6 README 用可执行命令替换文字描述**

```markdown
### 本地全栈启动

    docker compose -f docker-compose.dev.yml up -d postgres   # 首次 / 重置数据库后
    uv run alembic upgrade head                               # 新库
    # 若该库曾被 server 以 AUTH_MODE=local 启动过（create_all 建过表），改用：
    #   uv run alembic stamp head
    python -m uvicorn server.app:app --app-dir src --port 8000

    curl -s localhost:8000/api/health
    # 期望 {"status":"ok","db":true,"graph":true,"auth_mode":"local"}

前端开发：`cd web && npm run dev`（Vite 代理 `/api` → 127.0.0.1:8000）
生产：`cd web && npm run build`，FastAPI 自动挂载 `web/dist`（`WEB_DIST` 可覆盖路径）
```

### 验收

```powershell
docker compose -f docker-compose.dev.yml down -v            # 从零开始
docker compose -f docker-compose.dev.yml up -d postgres
uv run alembic upgrade head
uv run alembic current                                      # 期望 0002_usage_ledger (head)
uv run alembic upgrade head                                 # 期望幂等无报错
python -m uvicorn server.app:app --app-dir src --port 8000

# 另开一个终端：
curl -s localhost:8000/api/health                           # db/graph 均为 true
```

存量库路径另验：先用 `AUTH_MODE=local` 启动过 server 的库，执行 `uv run alembic stamp head` 后 `alembic current` 应显示 `0002_usage_ledger (head)`。

### 回滚

改动均集中在配置与两个函数：

```powershell
git checkout -- alembic.ini .env.example src/server/app.py src/server/deps.py src/server/db.py
```

`ensure_env_loaded` 幂等且 `override=False`，不会污染 shell 环境；`ODR_ENV_FILE` 未设置时行为等价于"找不到 .env 就不加载"。

### 风险

- `create_all` 与 alembic 并存是长期隐患：**建议 P1 的 0003 迁移落地时删除 `db.py` 的 `create_all` 兜底**，只留 alembic，彻底消除 schema 漂移。
- `.env` 与 `.env.example` 必须手工同步一次（`.env` 被忽略，不入库）。

---

## B4 · 模型与搜索配置（第 5 环）

### 完成定义（DoD）

`.env.example` 覆盖全部实际被读取的变量；按 README 选一档配好后，四个角色模型与搜索工具**都能实际调用成功**；有一条 <2 分钟、低成本的真研究验收。

### 事实基线

- `configuration.py:256`：`os.environ.get(field_name.upper(), configurable.get(field_name))` → **大写环境变量优先于请求参数**（env > `configurable` > Pydantic 默认）。
- 默认模型 `openai:gpt-4.1` / `gpt-4.1-mini`（`configuration.py:122,154,174,194`）；而 `.env:10` 的 `OPENAI_BASE_URL` 指向 DashScope 兼容端点 → **默认模型在该端点不存在**。
- `.env:1 OPENAI_API_KEY=` **为空字符串**，真密钥在 `DASHSCOPE_API_KEY`（`.env:9`）→ `utils.py:919 os.getenv("OPENAI_API_KEY")` 得到 `""` → 认证失败。
- `.env:4 TAVILY_API_KEY=` 为空，而 `search_api` 默认 `TAVILY` → **首次搜索必失败**。
- `utils.py:902-924` 只认 `openai:` / `anthropic:` / `google*` 前缀，其他前缀**返回 `None`**。
- `.env.example` 未文档化 14 个实际读取的变量（含全部模型名与预算旋钮）。

### 步骤

**Step 1 `.env.example` 增加四段（放在 `GET_API_KEYS_FROM_CONFIG` 之后、`DATABASE_URL` 之前或之后均可）**

```dotenv
# ---------------------------------------------------------------------------
# Models (role-based). Configuration reads the UPPERCASE env var with priority
# OVER per-request `configurable` values (configuration.py:256).
# Format: "<provider>:<model>", provider in {openai, anthropic, google_genai, deepseek}
# ---------------------------------------------------------------------------
SUMMARIZATION_MODEL=openai:gpt-4.1-mini
RESEARCH_MODEL=openai:gpt-4.1
COMPRESSION_MODEL=openai:gpt-4.1
FINAL_REPORT_MODEL=openai:gpt-4.1

# --- Alternative: any OpenAI-compatible endpoint (DashScope / vLLM / proxy) ---
# Put the VENDOR key into OPENAI_API_KEY (openai:* models read that variable,
# utils.py:918-919), set the base URL, then rename the four models above to ones
# that endpoint actually serves. Verify BEFORE trusting any name:
#   curl -s %OPENAI_BASE_URL%/models -H "Authorization: Bearer %OPENAI_API_KEY%"
# OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# OPENAI_API_KEY=
# RESEARCH_MODEL=openai:qwen-max
# COMPRESSION_MODEL=openai:qwen-plus
# FINAL_REPORT_MODEL=openai:qwen-max
# SUMMARIZATION_MODEL=openai:qwen-turbo

# Max output tokens per role (cost ceiling). Defaults shown.
SUMMARIZATION_MODEL_MAX_TOKENS=8192
RESEARCH_MODEL_MAX_TOKENS=10000
COMPRESSION_MODEL_MAX_TOKENS=8192
FINAL_REPORT_MODEL_MAX_TOKENS=10000

# ---------------------------------------------------------------------------
# Search + retrieval
# ---------------------------------------------------------------------------
# Allowed: tavily | openai | anthropic | none   (default: tavily)
SEARCH_API=tavily
# Comma-separated pluggable retrievers: duckduckgo, arxiv, local_docs
# (retrievers.py:179-183). Disabled when empty.
# Keyless fallback when you have no search API key:
#   SEARCH_API=none
#   EXTRA_RETRIEVERS=duckduckgo
EXTRA_RETRIEVERS=

# ---------------------------------------------------------------------------
# Research budget knobs (defaults shown; AUTH_MODE=local clamps to 1 / 2)
# ---------------------------------------------------------------------------
MAX_CONCURRENT_RESEARCH_UNITS=5
MAX_RESEARCHER_ITERATIONS=6
MAX_REACT_TOOL_CALLS=10
MAX_STRUCTURED_OUTPUT_RETRIES=3
ALLOW_CLARIFICATION=true
MAX_CONTENT_LENGTH=50000

# ---------------------------------------------------------------------------
# Local documents (RAG) + charts
# ---------------------------------------------------------------------------
ODR_DOCS_DIR=data/docs
# provider:model — only "openai:" is implemented today (docstore.py:33-36)
EMBEDDING_MODEL=openai:text-embedding-3-small
ODR_CHARTS_DIR=data/charts
CHART_MODEL=openai:gpt-4.1-mini
```

**Step 2 选一档落到本地 `.env`（三选一）**

档 A —— 官方 OpenAI（改动最小、最稳）：`OPENAI_API_KEY=sk-...`、删除 `OPENAI_BASE_URL`、`TAVILY_API_KEY=tvly-...`。

档 B —— OpenAI 兼容端点（与当前 `.env` 意图一致）：

```dotenv
OPENAI_API_KEY=<DASHSCOPE_API_KEY 的值>        # 关键：不能留空
OPENAI_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
RESEARCH_MODEL=openai:qwen-max
COMPRESSION_MODEL=openai:qwen-plus
FINAL_REPORT_MODEL=openai:qwen-max
SUMMARIZATION_MODEL=openai:qwen-turbo
EMBEDDING_MODEL=openai:text-embedding-v3
SEARCH_API=none
EXTRA_RETRIEVERS=duckduckgo
```

**先探活再定名**（模型名与 embedding 名随账号/区域不同，不要照抄）：

```powershell
curl -s "$env:OPENAI_BASE_URL/models" -H "Authorization: Bearer $env:OPENAI_API_KEY"
curl -s "$env:OPENAI_BASE_URL/embeddings" -H "Authorization: Bearer $env:OPENAI_API_KEY" `
     -H "Content-Type: application/json" -d '{"model":"text-embedding-v3","input":"ping"}'
```

档 C —— DeepSeek 官方（`langchain-deepseek` 已是依赖），需 2 行补丁，因为 `utils.py:902-924` 对其他前缀返回 `None`：

`src/open_deep_research/utils.py`，在 `google` 分支之后的两处（`GET_API_KEYS_FROM_CONFIG=true` 分支与 else 分支）各加：

```python
        elif model_name.startswith("deepseek"):
            return (api_keys.get("DEEPSEEK_API_KEY") if should_get_from_config.lower() == "true"
                    else os.getenv("DEEPSEEK_API_KEY"))
```

更清晰的做法是抽出映射表，两条分支共用：

```python
_KEY_ENV = {
    "openai": "OPENAI_API_KEY",
    "anthropic": "ANTHROPIC_API_KEY",
    "google": "GOOGLE_API_KEY",
    "deepseek": "DEEPSEEK_API_KEY",
}
```

`.env`：`DEEPSEEK_API_KEY=...`，四个模型写 `deepseek:deepseek-chat`。

**Step 3 新增探活脚本 `scripts/check_models.py`**

```python
"""Probe every configured role model + the assembled search tools. Costs ~0."""
from __future__ import annotations

import asyncio
import os
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "src"))

from dotenv import load_dotenv  # noqa: E402

load_dotenv(ROOT / ".env", override=False)

from langchain.chat_models import init_chat_model  # noqa: E402

from open_deep_research.configuration import Configuration, SearchAPI  # noqa: E402
from open_deep_research.utils import get_all_tools  # noqa: E402

ROLES = ("summarization_model", "research_model", "compression_model", "final_report_model")


def probe(role: str, model: str) -> bool:
    """One-token round trip against a role model."""
    try:
        init_chat_model(model, max_tokens=16).invoke("ping")
        print(f"[OK ] {role}: {model}")
        return True
    except Exception as exc:  # noqa: BLE001 — surfaced to the operator
        print(f"[FAIL] {role}: {model} -> {type(exc).__name__}: {str(exc)[:200]}")
        return False


async def main() -> int:
    cfg = Configuration.from_runnable_config(None)
    ok = all(probe(role, getattr(cfg, role)) for role in ROLES)

    print(f"search_api = {cfg.search_api.value}")
    try:
        tools = await get_all_tools({"configurable": {}})
        names = [getattr(t, "name", None) or t.get("name", "?") for t in tools]
        print(f"[OK ] tools: {names}")
        # ResearchComplete + think_tool are always present; a retriever must be too
        ok = ok and len(tools) > 2
    except Exception as exc:  # noqa: BLE001
        print(f"[FAIL] tool assembly -> {type(exc).__name__}: {str(exc)[:200]}")
        ok = False

    if cfg.search_api is SearchAPI.TAVILY:
        has_key = bool(os.environ.get("TAVILY_API_KEY"))
        print(f"[{'OK ' if has_key else 'FAIL'}] TAVILY_API_KEY")
        ok = ok and has_key
    return 0 if ok else 1


if __name__ == "__main__":
    raise SystemExit(asyncio.run(main()))
```

**Step 4 低成本端到端验收（<2 分钟，`AUTH_MODE=local` 已被 `deps.apply_cost_caps` 钳到 1 并发 / 2 轮）**

```powershell
$body = @{ question = '用一句话说明 LangGraph checkpointer 的作用' } | ConvertTo-Json
$sid  = (curl -s -X POST localhost:8000/api/sessions `
         -H 'Content-Type: application/json' -d $body | ConvertFrom-Json).id

curl -N -X POST "localhost:8000/api/sessions/$sid/runs/stream" `
     -H 'Content-Type: application/json' `
     -d '{"configurable":{"max_react_tool_calls":2}}'
```

期望：出现 `event: node` 序列，最后 `event: done`，`status` 为 `completed`（有报告）或 `awaiting_input`（澄清问题）；**不允许出现 `event: error`**。

### 验收

```powershell
python scripts/check_models.py       # 期望 exit 0 + 四行 [OK] + tools 列表非空
python scripts/env_check.py          # 期望 exit 0
# Step 4 的 SSE 冒烟：done 事件，无 error 事件
```

### 回滚

- `.env.example`：`git checkout -- .env.example`。
- 本地 `.env`：自行备份后还原。
- 档 C 的 `utils.py` 补丁：单独 commit，`git revert <sha>` 即可。
- 两个脚本删除不影响运行时。

### 风险

- DashScope / 第三方兼容端点的**模型名与 embedding 名随账号与区域变化**，必须用 Step 2 的 curl 探活确认。
- `openai:` 前缀 + 自定义 base URL 时，token 截断判定仍按 tiktoken `cl100k_base` 估算，与实际计费口径不同 —— 只影响截断阈值，不影响可用性。
- 档 B 的 `EXTRA_RETRIEVERS=duckduckgo` 依赖 `duckduckgo-search`（`pyproject` 钉 `>=3.0.0`，上游已改名 `ddgs`）；若网络不可达，改走 compose 中已就绪的 SearXNG（需先写 adapter，属 P4-4.3 范围）。
- `env > configurable` 的优先级意味着：一旦在 `.env` 里写了模型名，**按会话切换模型就失效**。单用户本地部署无碍；多租户前需重新评估该优先级。

---

## 5. 全部文件变更汇总

| 文件 | 动作 | 归属 |
|---|---|---|
| `.gitignore` | 追加 8 条忽略规则（`.venv/`、`data/`、`docs.zip`、`build_pdf` 产物、`.codebuddy/`、`pytest-cache-files-*/`、`.uv-cache/`） | B1 |
| `.gitattributes`（新） | `* text=auto eol=lf` 等 | B1 |
| 仓库根 `pytest-cache-files-*` | takeown + icacls 后删除 | B1 |
| `scripts/env_check.py`（新） | 环境自检，exit code 可进 CI | B2 |
| `.python-version`（新） | 钉解释器（3.11 或 3.13） | B2 |
| `pyproject.toml` | 追加 `[tool.pytest.ini_options]` | B2 |
| `src/server/deps.py` | 新增 `ensure_env_loaded()`（幂等、路径无关） | B3 |
| `src/server/app.py` | lifespan 首行调用 + import 调整 | B3 |
| `alembic.ini` | `prepend_sys_path = %(here)s/src`；`sqlalchemy.url =`（清空） | B3 |
| `src/server/db.py` | create_all 日志提示 `alembic stamp head` | B3 |
| `docker-compose.dev.yml` | 修正过期注释 | B3 |
| `.env.example` | DSN 对齐 compose + 模型/搜索/预算/本地文档四段 | B3 + B4 |
| `.env`（本地，不入库） | DSN + 模型名 + `OPENAI_API_KEY` 密钥映射 | B3 + B4 |
| `scripts/dev_up.ps1`（新） | 一键冷启动（PG → 迁移 → server） | B3 |
| `scripts/check_models.py`（新） | 四角色模型 + 工具装配探活 | B4 |
| `src/open_deep_research/utils.py` | 仅档 C：`get_api_key_for_model` 加 provider 映射 | B4 |
| `README.md` | 环境自检 + 三步全栈启动 + 前端构建 + 存量库 stamp 说明 | B2 + B3 |
| `CLAUDE.md` | 重写：双入口、两条启动命令、测试命令、"不要动内核"约束 | B1（随 Commit C） |